# LLM Pipeline — standard Python environment
# Install with: pip install -r requirements.txt

# Structured LLM extraction (Gemini via OpenAI-compatible API)
instructor>=1.4.0
openai>=1.30.0

# Pydantic schemas (used by llm_pipeline/generator.py and schemas.py)
pydantic>=2.0.0

# COCO-to-YOLO post-processing (scripts/coco_to_yolo.py)
numpy>=1.24.0
Pillow>=10.0.0

# Gradio UI (ui/app.py) — kurulum: aynı yorumlayıcı ile `python3 -m pip install -r requirements.txt`
gradio>=6.0,<7

# RAG system (rag_system/)
chromadb>=0.4.0
sentence-transformers>=2.2.0
beautifulsoup4>=4.12.0
requests>=2.31.0
